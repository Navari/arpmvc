{
  "name": "arpm/mini-mvc",
  "scripts": {
    "first:start": "composer install && composer start",
    "start" : "php -S localhost:8000 -t public"
  },
  "autoload": {
    "psr-4": {
      "App\\": "app/"
    },
    "classmap": [
      "app/"
    ]
  }
}
